import React, { useEffect } from "react";
const URL = "ws://tvilling.digital:1337";

const Socket2 = props => {
  useEffect(() => {}, []);

  //const ws = new WebSocket(URL);
  //ws.binaryType = "arraybuffer";

  return <div>Socket2</div>;
};

export default Socket2;
